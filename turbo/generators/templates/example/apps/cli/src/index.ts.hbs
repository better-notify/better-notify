import { createClient } from '@emailrpc/core'; import { mockProvider } from '@emailrpc/core/test';
import { emails } from '@{{kebab name}}/emails'; const provider = mockProvider(); const mail =
createClient({ router: emails, providers: [{ name: 'mock', provider, priority: 1 }], }); const
result = await mail.hello.send({ to: 'someone@example.com', input: { name: 'World' }, });
console.log('Message ID:', result.messageId); console.log('To: ', result.envelope.to.join(', '));
console.log('---'); console.log('Subject: ', provider.sent[0]!.subject);
console.log(provider.sent[0]!.text);